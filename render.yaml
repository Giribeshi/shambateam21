services:
  - type: web
    name: shambasmart
    env: node
    plan: free
    region: frankfurt
    buildCommand: bash build.sh
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: CI
        value: "false"
      - key: NODE_OPTIONS
        value: "--max_old_space_size=512"
      - key: JWT_SECRET
        generateValue: true
    healthCheckPath: /api
    autoDeploy: true
